variable:
una variable es un lugar donde podemos almacenar informacion, con un  valor u objetivo determinado que se le podra dar uso en el momento deseado, 
y tambien lo podremos cambiar en el momento que queramos.

var a = 7
a = 7


strings:
es una cadena de letras que se definen usando las comillas "" o ´´.
String = "soy hector"


Valores booleanos:
probienen de los binarios y se usan como verdadero o falso, es decir que solo ofrece dos opciones 0 o 1.

3<1 = false
3>1 = true

Funciones:
es una manera de organizar y reutilizar el codigo ya sea para mejorar procesos o para poder usarlo muchas veces.
las funciones estan acompañadas de argumentos, basicamnete son variables o datos que me definen lo que la funciuon me entregara como resultado.

function devolverString(str) {
 return (str);
  }
  devolverString('str');



Declaraciones: son condiciones que se usan en las funciones y se generan por medio preguntas por utilizan el      if = si

if (edad 32) {
    console.log (si)
else 
    console.log (no)
}